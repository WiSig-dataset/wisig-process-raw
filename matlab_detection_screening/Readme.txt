The raw files for a single day need to be placed in a folder called "data" as downloaded from google drive. 

You need to run the script "process_multirx.m"
It takes about 5 hours to run.

The whole packets are saved as mat files in a folder called "packets".
